# SynthGraph — Roadmap

> **Who this is for:** Anyone curious about the current release status and where the project is headed.

**V1 (current)** — PostgreSQL parser, graph-based dependency resolution, full constraint-aware generation, SQL/CSV export, `generate`/`inspect` CLI, web app with schema graph visualization, generation pipeline, and job history.

Future plans have moved to [`docs/Future-Plan.md`](Future-Plan.md).
